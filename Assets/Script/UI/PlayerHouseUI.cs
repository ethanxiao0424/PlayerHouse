using Sportvida;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerHouseUI : PageBase
{

    public ButtonUI itemBtn;

    protected override void Start()
    {
        //base.Start();
        //itemBtn.GetButton().onClick.AddListener(() => OpenItems(true));
        //tipsPage.GetButton().onClick.AddListener(() => OpenTips(false));
        //SkillsBtn.GetButton().onClick.AddListener(() => OpenSkills(true));
        //restartBtn.onClick.AddListener(() => IsRestart = true);
    }


    public void OpenItems(bool _IsOpen)
    {

    }
}
